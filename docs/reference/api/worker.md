# synapse_worker

The Edge Worker: source discovery (including `CodexSource`), compaction, the
tick loop (compact -> triage -> distil -> push), local bindings, and the
write-ahead-logged HTTP sink to the orchestrator.

::: synapse_worker
