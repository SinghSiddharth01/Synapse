# synapse_orchestrator

The single-egress local process: the MCP surface (the six `@server.tool`
entries), the producer endpoint, local binding resolution, and the arrival
briefing.

::: synapse_orchestrator
