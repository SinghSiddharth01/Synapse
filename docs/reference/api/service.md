# synapse_service

The shared store: the append-only Finding Log and its fold, semantic merge
and synthesis (with the derived output budget and merge-rate governor),
retrieval/suppression, and the `/v1` HTTP API.

::: synapse_service
