# synapse_providers

Model-provider adapters (fake, AIC100/NPU, and the GenieX-facing HTTP client)
behind one interface the distiller and synthesis paths call without caring
which backend is live.

::: synapse_providers
