// Template for secrets.jsonc — copy to secrets.jsonc at the repo root and
// fill in your values. secrets.jsonc is gitignored and must never be committed.
{
  // Cirrascale Inference Cloud (Cloud AI 100)
  "inference_cloud": {
    "api_key": "<your Cirrascale Inference Cloud API key>",
    "base_url": "https://aisuite.cirrascale.com/apis/v2",
    "model": "Llama-3.1-8B"
  },

  // Optional: Anthropic key for the Claude baseline/judge provider
  "anthropic": {
    "api_key": ""
  },

  // Optional: QUAD MCP bearer token
  "quad": {
    "mcp_token": ""
  }
}
