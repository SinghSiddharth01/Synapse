## 1. Aligned with the transcript

- **The core rubric line is hit head-on.** Transcript line 116 asks for "one-command install/run from GitHub, shown live to the 'joining' teammate as their first-time experience." §3's `curl … | sh` / `irm … | iex` from the raw GitHub URL, plus the double-clickable `install.bat` shim, is precisely that asset, and the `.bat` specifically removes the execution-policy stall from a live, unrehearsed moment — which is what line 204-206 (open-house rounds, families/managers/judges roaming) actually needs.
- **The non-goal list (§5) is the single best part of the scope.** Transcript line 184 — "keep any live portion to the most basic/robust interactions only" — argues directly for not rewriting `serve_local.py`, not packaging, not provisioning NPU, not shipping Docker. Verified: `claim_ports` (`scripts/serve_local.py:145-177`), health-wait and the teammate-command print (`:479-509`) already exist; duplicating them would create a second startup contract the night before submission.
- **Windows emphasis is proportionate, not paranoia.** The Snapdragon story (transcript 46-92) means the X Elite is on stage; the joining teammate plausibly *is* the Windows box. I confirmed the bug: `scripts/serve_local.py:482` prints `orchestr.  …/mcp  ← point Claude Code here` with U+2190, and it is the exact line the joining teammate needs. Under redirection on cp1252 that line raises before it prints. This is a real demo-killer, correctly sized.
- **P5's "always your own 127.0.0.1:8787" rule** is not ceremony — `scripts/serve_local.py:494-509` spells out that a teammate pointing at the host's orchestrator gets findings stamped as the host's and suppressed from themselves. That would break the awareness beat (transcript Step 5) in a way that looks like the product not working.
- **The acceptance criterion terminates in the right place** (§4 last paragraph): doctor all-PASS *and* the joiner's Claude Code answering with a teammate's name attached — i.e. transcript Steps 4-5, not "installer exited 0."
- **`secrets.example.jsonc`** is a genuine hole (`.gitignore` promises it; `git ls-files` has no match) and it blocks the submission requirement at transcript line 265-266 ("from-scratch setup instructions").

## 2. Misalignments, gaps, over-scope

**(a) `scripts/doctor.py` at 14 checks is the night's biggest over-scope risk.** Checks 7-12 (service reachable, MCP `initialize`, tool listing, binding introspection, session-live, round-trip query) re-prove things `serve_local.py` already proves by successfully printing its banner. Worse, check 12 ships with a documented confusion — "0 results from your own contributor is a PASS" — which is exactly the kind of ambiguous output that reads as broken on a projector. The rubric buys *ease of install*, not diagnostic depth.

**(b) The joiner's "one command" is not actually one command.** The rubric line is about the *joining teammate's first-time experience*. Under this scope the joiner must hand-transcribe three values (`--service-url`, `--shared-id`, `--contributor`) from the host. Nothing in §3 shows the `curl | sh` form *with arguments* — §3 shows `curl … | sh` bare, and `./install.sh --flags` from a checkout, never the combination, which is the demo case. `sh` needs `-s --` to forward args through a pipe; that is unspecified.

**(c) `--live` is missing.** `scripts/serve_local.py:218` defines `--live` (proxy the model seam to a real cloud model), and `README.md:88` documents it. It is the flag that puts the Cirrascale 70B — the transcript's AI100 pairing (lines 68-69) and the successful rehearsal at line 236-240 — into the demo. §P7's arm table and §3's flag mapping both omit it, as they omit `--distiller-model` / `--synthesizer-model`, which the 8B fallback (line 240) needs.

**(d) Blind to transcript Steps 1-2 and §8's asset bundle.** Step 1 has the host creating a session via a **slash command** (`/create`); Step 2 and lines 199-206 make the presentation view / Global View part of the same asset that must work at the open house. The repo has no `commands/` directory — `packs/claude-code/` holds only `skills/synapse-shared-memory/SKILL.md`, `hooks/freshness_pointer.py`, `settings-snippet.json`. P5 hardcodes copying *the skill*, so if a parallel workstream lands `/create` or a dashboard tonight, P5 needs re-editing at 3am.

**(e) §2 item 4 and half the codepoint table are analysis the night doesn't need.** The pytest-env aside, and the rows for `debug_server.py` / `debug.py` / `calibrate_prompt.py` / `demo_say.py` (all off the one-command run path), are dead weight the scope itself then declares out of scope in §5.

**(f) P4's `git check-ignore` abort is a false-failure generator.** A teammate who downloads a GitHub zip instead of cloning has no `.git`; `git check-ignore` exits non-zero and the installer aborts on a perfectly good machine. "Abort with a message" is the wrong default here.

**(g) P3/P2 `git pull --ff-only` can silently defeat the fallback plan.** Transcript line 241 says a pinned `demo-fallback` branch exists and is part of the maturity story. An installer that pulls on every run will either fail on a dirty checkout or fight an operator who has deliberately parked on the fallback.

**(h) No CI hazard, contrary to what §5 implies needs saying.** `.github/workflows/ci.yml` runs only `uv sync`, `uv run pytest -q`, and `rehearse_demo.py` — no shell/PowerShell linting, no root-file globs. Adding `install.sh` / `install.ps1` / `install.bat` cannot break the "build passing" badge the transcript asks for (line 120). The "Not CI" non-goal is safe as written; no action needed, but the scope should stop treating it as an open question.

## 3. Concrete corrections

1. **Cut doctor to ~8 checks and split it by phase.** Keep 1-6 (uv, interpreter/ARM64, `mcp==1.9.4`, Unicode canary, ports, secrets-blocks-present) and 13-14 (awareness pack, `claude mcp list`) as the *pre-flight* the installer runs before starting anything. Delete 7-12 outright: `serve_local.py` printing its banner with a shared-id already asserts service-reachable, orchestrator-up, session-live and binding-written, and `scripts/verify_instructions.py` already exists standalone for anyone who wants the SENTINEL probe. `--doctor-only` then becomes the *whole* doctor, not a subset — one code path, less to get wrong at 3am.
2. **Make the joiner literally one paste.** Specify `install.sh` to accept args through the pipe (`curl -LsSf …/install.sh | sh -s -- --service-url … --shared-id … --contributor akhil`) and `install.ps1` equivalently (`&([scriptblock]::Create((irm …))) -ServiceUrl … -SharedId …`). Then extend the existing edit to `scripts/serve_local.py:500-509` so the host's printed teammate block emits *that* full one-liner, both OS variants, ready to paste into chat. This is the deliverable the rubric line describes; today it is implied but not specified anywhere in the scope.
3. **Add `--live/-Live`, `--distiller-model`, `--synthesizer-model`** to the P7 arm table and §3 flag mapping, and state the pass-through rule as the default for *unknown* flags so the two never drift again.
4. **Generalize the pack copy.** P5 should copy `packs/claude-code/skills/` **and** `packs/claude-code/commands/` and `packs/claude-code/agents/` if present (glob, skip-if-absent), rather than naming `synapse-shared-memory`. Zero cost tonight, and it absorbs a `/create` command or dashboard shipped by a parallel workstream without a second edit.
5. **Downgrade the check-ignore abort to a warning** when `git rev-parse --is-inside-work-tree` fails; abort only when inside a real repo *and* `secrets.jsonc` is not ignored.
6. **Never pull by default.** P2: if a checkout exists, use it as-is and print the current branch/SHA; pull only under an explicit `--update`/`-Update` flag, and never switch branches. Add the current branch to doctor's output so an operator on `demo-fallback` can see it.
7. **Trim §2 to the run path**: ASCII the arrow at `serve_local.py:482`, add the `sys.stdout/stderr.reconfigure` guard to `serve_local.py`, `demo_local.py`, `doctor.py`, and set `PYTHONUTF8=1` + `chcp 65001` in `install.ps1`. Delete the rest of the table and item 4 from the scope so nobody implements them.
8. **Fold the submission checklist into the README edit already planned** (transcript 264-268): personal repo, team names+emails, from-scratch setup, run instructions, license. `LICENSE` exists and `README.md:63-95` already covers setup/run — this is a five-minute verification appended to an edit W8 is making anyway, and it protects 15 rubric points adjacent to the 20 W8 owns.

VERDICT: NEEDS-CHANGES