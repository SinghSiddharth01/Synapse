# synapse_contracts

The frozen contract package: `Finding`, `Attribution`, the Session/Session-status
schemas, and the enums both the worker and the service import — this is the
vocabulary every other package speaks.

::: synapse_contracts
